# Copy this file to .env and set values before running docker compose.
# Do not commit .env (already ignored by .gitignore).

POSTGRES_DB=exceldb
POSTGRES_USER=excel
POSTGRES_PASSWORD=change-this-password
